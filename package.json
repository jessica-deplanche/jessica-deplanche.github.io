{
  "name": "jessica-deplanche-portfolio",
  "version": "1.0.0",
  "description": "Portfolio de Jessica Deplanche - UI & Web Designer",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "node scripts/generate-banniere.js && node scripts/optimize-images.js && echo 'Static site ready'",
    "lint": "node --check server.js && node --check scripts/generate-banniere.js && node --check scripts/optimize-images.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "opentype.js": "^2.0.0",
    "sharp": "^0.35.4"
  }
}
